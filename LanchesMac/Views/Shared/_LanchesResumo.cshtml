@model Lanche


<div class="col-sm-4 col-md-4 col-lg-4">
    <p><img src="@Model.ImagemUrl" class="img-fluid" /></p>
    <h3>@Model.Preco.ToString("c")</h3>
    <h4>@Model.Nome</h4>
    <h5>@Model.DescricaoCurta</h5>
</div>
